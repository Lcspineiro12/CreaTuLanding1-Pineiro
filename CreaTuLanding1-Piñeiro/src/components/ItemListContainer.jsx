function ItemListContainer({ greeting }) {
  return (
    <section className="container">
      <h1>{greeting}</h1>
      {/* Aquí en futuras entregas se renderizará el catálogo */}
    </section>
  )
}

export default ItemListContainer